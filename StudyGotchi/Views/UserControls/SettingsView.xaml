<UserControl x:Class="StudyGotchi.Views.UserControls.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">
        <TextBlock Text="Name your Pet:" Foreground="{StaticResource DarkText}" Margin="0,0,0,5"/>
        <TextBox x:Name="TxtPetName" Margin="0,0,0,20"/>

        <TextBlock Text="Hunger Decay Speed:" Foreground="{StaticResource DarkText}" Margin="0,0,0,5"/>
        <Slider x:Name="SldDecaySpeed" Minimum="1" Maximum="10" Value="5" Margin="0,0,0,30"/>

        <Grid>
            <Button Content="Back" HorizontalAlignment="Left" Padding="20,10" Click="BtnBack_Click"/>
            <Button Content="Save &amp; Start" HorizontalAlignment="Right" Padding="20,10" Background="{StaticResource PrimaryPink}" Click="BtnSave_Click"/>
        </Grid>
    </StackPanel>
</UserControl>