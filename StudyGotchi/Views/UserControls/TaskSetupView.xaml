<UserControl x:Class="StudyGotchi.Views.UserControls.TaskSetupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBox x:Name="TxtTaskName" Width="200" Margin="0,0,10,0"/>
            <DatePicker x:Name="DpDeadline" Width="150" Margin="0,0,10,0"/>
            <Button Content="Add Task" Background="{StaticResource PrimaryPink}" Click="BtnAddTask_Click"/>
        </StackPanel>

        <ListBox x:Name="LstTasks" Grid.Row="1" Margin="50,0,50,0"/>

        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Button Content="Back" HorizontalAlignment="Left" Padding="20,10" Click="BtnBack_Click"/>
            <Button Content="Next" HorizontalAlignment="Right" Padding="20,10" Background="{StaticResource PrimaryPink}" Click="BtnNext_Click"/>
        </Grid>
    </Grid>
</UserControl>