<UserControl x:Class="StudyGotchi.Views.UserControls.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Image x:Name="ImgActivePet" Width="250" Height="250" RenderOptions.BitmapScalingMode="NearestNeighbor"/>

            <TextBlock Text="Hunger" Foreground="{StaticResource DarkText}" Margin="0,20,0,0"/>
            <ProgressBar x:Name="BarHunger" Height="20" Width="300" Maximum="100" Foreground="{StaticResource MintGreen}"/>

            <TextBlock x:Name="TxtLevel" Text="Level 1 - Baby" Foreground="{StaticResource DarkText}" Margin="0,10,0,0"/>
            <ProgressBar x:Name="BarXp" Height="10" Width="300" Maximum="100" Foreground="{StaticResource SoftLavender}"/>
        </StackPanel>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ListBox x:Name="LstActiveTasks" Grid.Row="0" Margin="0,0,0,20"/>

            <StackPanel Grid.Row="1">
                <Button Content="Widget Mode" Margin="0,5" Background="{StaticResource SoftLavender}" Click="BtnWidgetMode_Click"/>
                <Button Content="Settings" Margin="0,5" Click="BtnSettings_Click"/>
                <Button Content="End Session" Margin="0,5" Background="{StaticResource DangerRed}" Foreground="White" Click="BtnEndSession_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>