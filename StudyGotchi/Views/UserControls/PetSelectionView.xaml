<UserControl x:Class="StudyGotchi.Views.UserControls.PetSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:gif="http://wpfanimatedgif.codeplex.com"
             xmlns:conv="clr-namespace:StudyGotchi.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             RenderOptions.BitmapScalingMode="NearestNeighbor"
             UseLayoutRounding="True">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <conv:StringToImageSourceConverter x:Key="StringToImage"/>
    </UserControl.Resources>

    <Grid>
        <Image Source="/Assets/Background/bg.png" 
               Stretch="UniformToFill" />

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="180"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="100"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" VerticalAlignment="Top">
                <Image Source="/Assets/Background/logo.png" 
                       Height="120" 
                       HorizontalAlignment="Center"
                       RenderOptions.BitmapScalingMode="NearestNeighbor"/>

                <TextBlock Text="choose your partner!"
                           FontFamily="{StaticResource PixelifyFont}"
                           FontSize="24"
                           Foreground="#2E4A7B"
                           HorizontalAlignment="Center"
                           Margin="0,5,0,0"/>
            </StackPanel>


            <ListBox Grid.Row="1"
                     ItemsSource="{Binding AvailablePets}"
                     SelectedItem="{Binding SelectedPet}"
                     SelectionChanged="ListBox_SelectionChanged"
                     ItemContainerStyle="{StaticResource EggItemStyle}"
                     Background="Transparent"
                     BorderThickness="0"
                     VerticalAlignment="Center"
                     HorizontalAlignment="Center"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Disabled">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="3" IsItemsHost="True" Width="650"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Height="180" Width="150">
                            <Ellipse Width="80" Height="40" 
                                     Fill="{Binding TintColor}" 
                                     Opacity="0.5" 
                                     VerticalAlignment="Bottom" 
                                     Margin="0,0,0,10">
                                <Ellipse.Effect>
                                    <BlurEffect Radius="20"/>
                                </Ellipse.Effect>
                            </Ellipse>

                            <Image Source="{Binding SpritePath}" 
                                   Width="120" Height="120"
                                   VerticalAlignment="Bottom"
                                   HorizontalAlignment="Center"
                                   Stretch="Uniform"
                                   RenderOptions.BitmapScalingMode="NearestNeighbor"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Button Grid.Row="2"
                    Content="start"
                    Style="{StaticResource StudyGotchiButtonStyle}"
                    Command="{Binding ChoosePetCommand}"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Width="200"
                    Height="60"/>
        </Grid>
    </Grid>
</UserControl>