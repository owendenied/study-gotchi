<UserControl x:Class="StudyGotchi.Views.UserControls.PetSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource MainGradientBrush}"
             FontFamily="Segoe UI">

    <Grid Margin="48">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,8,0,28">
            <TextBlock FontSize="42" FontWeight="Bold" Foreground="#3A1F1A" HorizontalAlignment="Center">
                <Run Text="📚  "/>
                <Run Text="Study-Gotchi"/>
                <Run Text="  📚"/>
            </TextBlock>
            <TextBlock Text="Choose your partner!" FontSize="18" Foreground="#6E4E4A" HorizontalAlignment="Center" Margin="0,8,0,0"/>
        </StackPanel>

        <!-- Cards -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">

            <!-- Selected Card -->
            <Grid Margin="0,0,28,0">
                <Border Style="{StaticResource PetCardStyle}" BorderBrush="{StaticResource AccentCoral}" BorderThickness="4">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Border Width="160" Height="160" CornerRadius="80" Background="#FFF6F3" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,6,0,12">
                            <Image Source="/Assets/Sprites/bulbasaur.png" Width="120" Height="120" Stretch="Uniform"/>
                        </Border>
                        <TextBlock Text="Leafy" FontWeight="Bold" FontSize="18" Foreground="#3A1F1A" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                        <TextBlock Text="A gentle nature spirit" FontSize="13" Foreground="#8A6F6B" HorizontalAlignment="Center" Margin="0,6,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Check mark -->
                <Ellipse Width="34" Height="34" Fill="White" Stroke="{StaticResource AccentCoral}" StrokeThickness="3" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="-12,-12,0,0"/>
                <TextBlock Text="✔" FontSize="16" FontWeight="Bold" Foreground="{StaticResource AccentCoral}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="-6,-18,0,0"/>
            </Grid>

            <!-- Middle Card -->
            <Border Style="{StaticResource PetCardStyle}" Margin="0,0,28,0">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Border Width="160" Height="160" CornerRadius="80" Background="#FFF8F8" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,6,0,12">
                        <Image Source="/Assets/Sprites/charmander.png" Width="120" Height="120" Stretch="Uniform"/>
                    </Border>
                    <TextBlock Text="Sunny" FontWeight="Bold" FontSize="18" Foreground="#3A1F1A" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                    <TextBlock Text="Warm and full of cheer" FontSize="13" Foreground="#8A6F6B" HorizontalAlignment="Center" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>

            <!-- Right Card -->
            <Border Style="{StaticResource PetCardStyle}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Border Width="160" Height="160" CornerRadius="80" Background="#FBFBFF" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,6,0,12">
                        <Image Source="/Assets/Sprites/squirtle.png" Width="120" Height="120" Stretch="Uniform"/>
                    </Border>
                    <TextBlock Text="Starry" FontWeight="Bold" FontSize="18" Foreground="#3A1F1A" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                    <TextBlock Text="Dreamy and curious" FontSize="13" Foreground="#8A6F6B" HorizontalAlignment="Center" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>

        </StackPanel>

        <!-- Action button -->
        <Button Grid.Row="2" Content="I Choose You!" Style="{StaticResource AccentPillButtonStyle}" HorizontalAlignment="Center" Margin="0,30,0,10" Padding="40,12" Click="BtnChoose_Click"/>

    </Grid>
</UserControl>
